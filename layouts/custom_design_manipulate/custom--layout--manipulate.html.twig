{% if content %}
  <div{{ attributes }}>
    {% if content.content %}
      <div {{ region_attributes.content.addClass('layout__region', 'row') }}>
        {{ content.content }}
      </div>
    {% endif %}
  </div>
{% endif %}